<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.locinder">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/passport"
        android:label="@string/app_name"
        android:longClickable="false"
        android:roundIcon="@drawable/passport"
        android:supportsRtl="true"
        android:theme="@style/Theme.Locinder">

        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".MapsActivity"
            android:exported="false"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".moreInfo"
            android:exported="false" />
        <activity
            android:name=".Presentation.Addphotos"
            android:exported="false" />
        <activity
            android:name=".Presentation.AddSite"
            android:exported="false" />
        <activity android:name=".Locale" />
        <activity
            android:name=".splashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ResetPassword" />
        <activity android:name=".Presentation.ChatRoom" />
        <activity android:name=".ShowChats" />
        <activity android:name=".Presentation.Events" />
        <activity android:name=".Presentation.FinishOrganizing" />
        <activity android:name=".Organizer" />
        <activity android:name=".Presentation.ControlActivity" />
        <activity android:name=".ReviewActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".slideActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>